<UserControl x:Class="AutoNelkuliIskolaWPF.UserControls.EditStudentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="White" Padding="20" CornerRadius="8">

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <TextBlock Text="Tanulók módosítása"
                           FontSize="22"
                           Margin="0,0,0,15"/>

                <!-- Example repeated data -->
                <StackPanel Margin="0,5">
                    <TextBox Text="Név"/>
                    <TextBox Text="Kor"/>
                    <Button Margin="0,5">Mentés</Button>
                </StackPanel>

                <StackPanel Margin="0,5">
                    <TextBox Text="Név"/>
                    <TextBox Text="Kor"/>
                    <Button Margin="0,5">Mentés</Button>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

    </Border>

</UserControl>